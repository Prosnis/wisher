const path = {
  register: '/register',
  auth: '/auth',
  user: '/user',
  main: '/',
  card: '/card',
  feed: '/feed',
  invitationCard: '/iCard',
  invitations: '/invitations',
  settings: '/settings',
  cards: '/cards',
  cardAdd: '/createCard',
}

export default path
